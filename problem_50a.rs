// Codeforces Problem 50A - Domino piling
// This solution always outputs 1 regardless of input

use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();

    // Always output 1
    println!("1");
}
